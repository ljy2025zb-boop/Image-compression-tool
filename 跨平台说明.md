# 跨平台开发和打包说明

## 📋 概述

本项目是一个**跨平台**的图片压缩工具，支持在 Windows 和 macOS 上运行和打包。

## ✅ 支持的平台

| 功能 | Windows | macOS | Linux |
|------|---------|-------|-------|
| 开发 | ✅ | ✅ | ✅ |
| 运行 | ✅ | ✅ | ✅ |
| 打包 .exe | ✅ | ❌ | ❌ |
| 打包 .dmg | ❌ | ✅ | ❌ |

## 🎯 使用场景

### 场景 1：Windows 用户（最常见）

**你的情况**：在 Windows 上开发，需要打包为 macOS 的 .dmg

**解决方案**：

#### 方案 A：使用 GitHub Actions（推荐 ⭐）

1. **在 Windows 上开发和测试**
   ```bash
   pip install -r requirements.txt
   python main.py
   ```

2. **推送代码到 GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/你的用户名/项目名.git
   git push -u origin main
   ```

3. **GitHub 自动打包**
   - GitHub Actions 会自动在 macOS 环境中打包
   - 等待几分钟后，在 Actions 页面下载 DMG 文件

4. **下载打包好的文件**
   - 访问：https://github.com/你的用户名/项目名/actions
   - 点击最新的 workflow run
   - 下载 "ImageCompressor-macOS" artifact

**优点**：
- ✅ 完全免费
- ✅ 自动化，无需手动操作
- ✅ 不需要 Mac 电脑
- ✅ 可重复使用

#### 方案 B：借用 Mac 电脑

1. **在 Windows 上开发**
   ```bash
   python main.py  # 测试功能
   ```

2. **传输到 Mac**
   - 使用 U 盘
   - 使用网络共享
   - 使用云盘（OneDrive/Google Drive）

3. **在 Mac 上打包**
   ```bash
   cd /path/to/project
   pip3 install -r requirements.txt
   chmod +x build_dmg.sh
   ./build_dmg.sh
   ```

4. **获取 DMG 文件**
   - 文件位置：`dist/ImageCompressor.dmg`

#### 方案 C：只打包 Windows 版本

如果你的用户主要是 Windows 用户：

```bash
# 在 Windows 上运行
build_windows.bat
```

生成的 `dist/ImageCompressor.exe` 可以直接分发给 Windows 用户。

---

### 场景 2：macOS 用户

**你的情况**：在 macOS 上开发

**操作步骤**：

```bash
# 1. 安装依赖
pip3 install -r requirements.txt

# 2. 运行应用
python3 main.py

# 3. 打包 DMG
chmod +x build_dmg.sh
./build_dmg.sh
```

生成的 `dist/ImageCompressor.dmg` 可以分发给其他 macOS 用户。

---

### 场景 3：同时支持 Windows 和 macOS

**方案 1：使用 GitHub Actions**

创建两个 workflow：
- `.github/workflows/build-macos.yml`（已提供）
- `.github/workflows/build-windows.yml`（可自行创建）

**方案 2：提供 Python 源码**

让用户自行运行：
```bash
pip install -r requirements.txt
python main.py
```

---

## 🔧 详细操作指南

### Windows 用户快速开始

1. **安装 Python**
   - 下载：https://www.python.org/downloads/
   - 安装时勾选 "Add Python to PATH"

2. **安装依赖**
   ```bash
   pip install -r requirements.txt
   ```

3. **运行应用**
   ```bash
   python main.py
   ```

4. **打包为 .exe（可选）**
   ```bash
   build_windows.bat
   ```

5. **获取 macOS 的 .dmg**
   - 使用 GitHub Actions（推荐）
   - 或借用 Mac 电脑

### macOS 用户快速开始

1. **安装依赖**
   ```bash
   pip3 install -r requirements.txt
   ```

2. **运行应用**
   ```bash
   python3 main.py
   ```

3. **打包为 .dmg**
   ```bash
   chmod +x build_dmg.sh
   ./build_dmg.sh
   ```

---

## 📦 打包文件对比

### Windows .exe

**生成方式**：
```bash
build_windows.bat
```

**特点**：
- ✅ 单个可执行文件
- ✅ 无需安装 Python
- ✅ 双击即可运行
- ⚠️ 文件较大（~100MB）
- ⚠️ 只能在 Windows 上运行

**分发方式**：
- 直接发送 .exe 文件
- 或压缩为 .zip 文件

### macOS .dmg

**生成方式**：
```bash
./build_dmg.sh
```

**特点**：
- ✅ 标准的 macOS 安装包
- ✅ 拖拽安装
- ✅ 原生外观
- ⚠️ 只能在 macOS 上运行

**分发方式**：
- 直接发送 .dmg 文件
- 或上传到网盘

---

## 🌐 GitHub Actions 详细说明

### 什么是 GitHub Actions？

GitHub Actions 是 GitHub 提供的免费 CI/CD 服务，可以在云端自动构建和测试代码。

### 如何使用？

1. **创建 GitHub 仓库**
   - 访问 https://github.com/new
   - 创建新仓库

2. **推送代码**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/你的用户名/项目名.git
   git push -u origin main
   ```

3. **查看构建状态**
   - 访问：https://github.com/你的用户名/项目名/actions
   - 等待构建完成（约 5-10 分钟）

4. **下载构建产物**
   - 点击最新的 workflow run
   - 在 "Artifacts" 部分下载 DMG 文件

### 配置文件

已提供的配置文件：
- `.github/workflows/build-macos.yml` - macOS 打包

你可以自行创建：
- `.github/workflows/build-windows.yml` - Windows 打包

---

## 🔍 常见问题

### Q1: 我在 Windows 上，能直接打包 macOS 应用吗？

**答**：不能。macOS 应用必须在 macOS 环境中打包。

**解决方案**：
- 使用 GitHub Actions（推荐）
- 借用 Mac 电脑
- 使用云端 macOS 服务

### Q2: GitHub Actions 是免费的吗？

**答**：是的，对于公开仓库完全免费。私有仓库每月有 2000 分钟的免费额度。

### Q3: 打包的文件为什么这么大？

**答**：因为包含了 Python 解释器和所有依赖库。这是正常的。

- Windows .exe：约 100-150 MB
- macOS .app：约 80-120 MB

### Q4: 可以减小文件大小吗？

**答**：可以，但会增加复杂度：
- 使用虚拟环境，只包含必需的库
- 使用 UPX 压缩
- 排除不必要的文件

### Q5: 用户需要安装什么吗？

**答**：
- **打包后的应用**：不需要，直接运行
- **Python 源码**：需要安装 Python 和依赖

### Q6: 如何更新应用？

**答**：
1. 修改代码
2. 重新打包
3. 分发新版本

或者使用自动更新机制（需要额外开发）。

---

## 📚 相关文档

- `README.md` - 完整使用说明
- `WINDOWS_GUIDE.md` - Windows 详细指南
- `WINDOWS_快速开始.txt` - Windows 快速指南
- `QUICKSTART.md` - 快速开始
- `DEVELOPMENT.md` - 开发者文档

---

## 💡 推荐工作流程

### 日常开发（Windows）

```bash
# 1. 修改代码
# 2. 测试功能
python main.py

# 3. 提交到 Git
git add .
git commit -m "Update features"
git push
```

### 发布新版本

```bash
# 1. 更新版本号
# 编辑 VERSION 文件

# 2. 更新 CHANGELOG.md
# 记录更新内容

# 3. 推送到 GitHub
git add .
git commit -m "Release v1.1.0"
git push

# 4. 等待 GitHub Actions 完成
# 5. 下载并分发打包文件
```

---

## 🎓 学习资源

- [GitHub Actions 文档](https://docs.github.com/actions)
- [PyInstaller 文档](https://pyinstaller.org/)
- [py2app 文档](https://py2app.readthedocs.io/)
- [Python 跨平台开发](https://docs.python.org/3/library/os.html)

---

**总结**：
- ✅ 代码是跨平台的，可以在任何系统上运行
- ✅ 在 Windows 上开发完全没问题
- ✅ 使用 GitHub Actions 可以轻松获得 macOS 的 .dmg
- ✅ 也可以打包为 Windows 的 .exe

**推荐方案**：在 Windows 上开发 + GitHub Actions 打包 macOS 版本
